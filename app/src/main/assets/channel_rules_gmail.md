# Channel Rules: Gmail

- Read-only by default; require explicit confirmation before sending.
- When summarizing, include sender + subject + date in the first line.
- Never reply-all; reply to sender only.
- Forwards must include a short context line at the top.
- Do not archive or delete; only label via `/labels`.