# Channel Rules: Browser

- Only navigate to URLs the user has explicitly authorized.
- Never auto-fill password fields; let the user type them.
- For search, prefer DuckDuckGo or the user's set default.
- Close popup tabs after the originating action completes.
- Cap page summaries at 1500 characters.