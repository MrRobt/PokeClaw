# Channel Rules: Phone

- Read-only by default; only send SMS or make calls with explicit approval.
- Do not place calls to numbers not in the user's contacts.
- For missed-call follow-up, use the user-set template only.
- Always confirm the destination before initiating.
- Log every placed call/sent SMS to the audit trail.