<?xml version="1.0" encoding="utf-8"?>
<!--
  Template picker activity layout
  (US-D-030-CHANNEL-CODE-TEMPLATE-FILTER). Top: ChipGroup for
  channel filter. Bottom: ListView of matching templates.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="12dp">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/template_picker_filter_label"
        android:textSize="13sp"
        android:textStyle="bold"
        android:paddingBottom="6dp" />

    <com.google.android.material.chip.ChipGroup
        android:id="@+id/cg_template_channels"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:singleSelection="true"
        app:selectionRequired="true"
        app:chipSpacingHorizontal="6dp"
        android:paddingBottom="12dp" />

    <ListView
        android:id="@+id/lv_templates"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:divider="@android:color/darker_gray"
        android:dividerHeight="0.5dp" />
</LinearLayout>
